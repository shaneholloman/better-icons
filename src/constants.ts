import packageJson from "../package.json" with { type: "json" };

export const PACKAGE_NAME = "better-icons";
export const VERSION = packageJson.version;
export const ICONIFY_API = "https://api.iconify.design";
